// This is a Server Component (no "use client") — it renders to HTML on the
// server. It's the "/" route. Placeholder for now; real content comes next.
export default function Home() {
  return (
    <main className="mx-auto max-w-[760px] px-7 py-20">
      <h1 className="text-3xl font-semibold text-ink">Next.js is running 🎉</h1>
      <p className="mt-2 text-muted">The migration begins here.</p>
    </main>
  )
}
